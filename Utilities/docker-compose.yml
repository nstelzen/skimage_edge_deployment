version: "3.3"
services:
  prod_ARM:
    image: nickstelzenmuller/skimage:ARM_prod
    privileged: true
    container_name: prod_ARM

    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix
      - /tmp/.docker.xauth-n-n:/tmp/.docker.xauth-n-n
      - /home/odroid/skimage_edge_deployment:/home
      - /etc/localtime:/etc/localtime:ro
      - /etc/timezone:/etc/timezone:ro

    working_dir: /home
    
    environment:
    - TZ=Europe/Paris
    - XAUTHORITY=/tmp/.docker.xauth-n 
    - DISPLAY=$DISPLAY
    # - SSH_AUTH_SOCK=/tmp/ssh_auth_sock  

    network_mode: "host"
